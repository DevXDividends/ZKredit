# Python
venv/
*/venv/
__pycache__/
*.pyc
*.egg-info/

# Backend runtime
backend/zkredit.db
*.db

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.env

# Foundry
contracts/cache/
contracts/out/
contracts/lib/

# EZKL circuit build artifacts (regenerable — settings.json/model.compiled etc.
# are cheap to rebuild from the ONNX model; SRS/proving keys are large and
# environment-specific, so don't commit them)
circuits/*/*.compiled
circuits/*/pk.key
circuits/*/vk.key
circuits/*/proof.json
circuits/*/witness.json
circuits/*/kzg*.srs

# OS / editor
.DS_Store
.vscode/
*.swp
